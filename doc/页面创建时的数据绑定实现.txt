一个页面包含多个模块
	每个模块有其唯一的数据来源
	每个模块获取数据的方式：
		List<xxx> list = request.getAttribute("唯一名字");
	“唯一名字”对应的哪个数据表的哪个域
		方案一：
			"唯一名字"初始值为"#CSVAULE",在绑定数据源时使用JS替换成 随机数
			然后将随机数与数据表和域绑定（在数据库中新建一张绑定表）
			提交字段：页面名+随机数+数据表+域
			库中：页面和随机数+数据表+域是一对多的关系
	需要哪些数据库，需要哪些域